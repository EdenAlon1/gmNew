<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp">
    <android.support.v7.widget.RecyclerView
        android:id="@android:id/list"
        android:scrollbars="vertical"
        android:clipToPadding="false"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <FrameLayout
        android:layout_gravity="bottom"
        android:id="@+id/confirm_button_container"
        android:background="@drawable/confirm_button_gradient"
        android:layout_width="match_parent"
        android:layout_height="@dimen/confirm_button_container_height">
        <Button
            android:enabled="false"
            android:layout_gravity="center"
            android:id="@+id/confirm_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:minHeight="48dp"
            android:text="@string/donation_donate_action_label"
            android:insetTop="0dp"
            android:insetBottom="0dp"/>
    </FrameLayout>
    <FrameLayout
        android:id="@+id/progress_background"
        android:background="@color/primary_background_color"
        android:focusable="true"
        android:clickable="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no">
        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:layout_gravity="center"
            android:id="@+id/progress"
            android:visibility="gone"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:indeterminate="true"
            app:trackColor="?attr/colorPrimaryBrandIcon"/>
    </FrameLayout>
</FrameLayout>
